# Shared upper bounds so a routine CI run cannot pick an untested major.
# Per-project requirements.txt keep their own floors (ONS is on pandas 3.x).
pandas>=2.2,<3.1
pyarrow>=16,<26
boto3>=1.34,<2
openpyxl>=3.1,<3.2
requests>=2.31,<3
markdown>=3.6,<4
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1
pytest>=8,<9
